# 循迹代码对比分析报告

## 项目概览

本报告对比分析了两个ESP32智能小车项目中的循迹代码：
- **line-following**：基础循迹功能项目
- **lnf_avoid**：增加了避障功能的循迹项目

## 主要发现

经过详细阅读和比较，两个项目中的循迹核心算法**基本相同**，但`lnf_avoid`项目在基础循迹功能上增加了障碍物避让功能。

---

## 相同部分

### 1. 循迹核心代码（`line_follow.c` 和 `line_follow.h`）

#### 状态定义完全相同
所有循迹状态枚举保持一致：
```c
typedef enum {
    LINE_FOLLOW_WAITING_LINE = 0,    // 等待检测到线
    LINE_FOLLOW_TRACKING,             // 正在循迹
    LINE_FOLLOW_CORNER_CANDIDATE,     // 角落候选
    LINE_FOLLOW_CORNER_ROTATE,       // 角落旋转
    LINE_FOLLOW_CORNER_EXIT,          // 角落退出
    LINE_FOLLOW_LOST_SEARCH,         // 丢失线搜索
    LINE_FOLLOW_STOPPED,             // 停止
} line_follow_state_t;
```

#### 算法逻辑完全一致
- **PID控制算法**：比例-积分-微分控制参数相同
  ```c
  #define LINE_BASE_FORWARD          160
  #define LINE_MIN_FORWARD           100
  #define LINE_KP                     70
  #define LINE_KD                     15
  #define LINE_TURN_LIMIT            250
  ```
- **角检测机制**：角落检测、旋转、退出逻辑相同
- **丢失线搜索**：搜索算法、方向控制、计数器逻辑相同

#### 数据结构完全相同
```c
typedef struct {
    line_follow_state_t state;
    int error;
    int forward;
    int turn;
    bool state_changed;
} line_follow_result_t;

typedef struct {
    line_follow_state_t state;
    int last_error;
    bool has_last_error;
    uint32_t lost_ms;
    uint32_t all_black_ms;
    uint32_t invalid_ms;
    // ... 其他字段完全相同
} line_follow_controller_t;
```

#### 关键函数完全相同
- `line_follow_init()` - 初始化循迹控制器
- `line_follow_update()` - 更新循迹逻辑，计算控制命令
- `line_follow_state_name()` - 状态名称获取

### 2. 基础硬件配置相同

#### 红外传感器配置
```c
#define IR_CHANNEL_4_GPIO  (9)
#define IR_CHANNEL_3_GPIO  (10)
#define IR_CHANNEL_2_GPIO  (11)
#define IR_CHANNEL_1_GPIO  (12)
#define IR_BLACK_LEVEL     0
#define IR_SAMPLE_PERIOD_MS        10
#define IR_DEBOUNCE_SAMPLE_COUNT    2
```

#### 电机控制配置
```c
// D24A/TB6612FNG motor outputs
#define MOTOR_LEFT_IN1_GPIO        41
#define MOTOR_LEFT_IN2_GPIO        42
#define MOTOR_LEFT_PWM_GPIO         2

#define MOTOR_RIGHT_IN1_GPIO       7
#define MOTOR_RIGHT_IN2_GPIO       6
#define MOTOR_RIGHT_PWM_GPIO       5
```

#### 编码器配置
```c
#define ENCODER_LEFT_A_GPIO       (40)
#define ENCODER_LEFT_B_GPIO       (39)
#define ENCODER_RIGHT_A_GPIO      (15)
#define ENCODER_RIGHT_B_GPIO      (16)
```

---

## 不同之处

### 1. 循迹应用逻辑（`main.c`）

#### line-following项目（纯循迹）
```c
const line_follow_result_t control = 
    line_follow_update(&controller, sensor.black_mask, elapsed_ms,
                       left_count, right_count);

result = drive_set_motion(control.forward, control.turn, &wheels);
```

#### lnf_avoid项目（循迹+避障）
```c
// 障碍物避让控制
const obstacle_avoidance_result_t avoid_control =
    obstacle_avoidance_update(&avoidance, &ultrasonic, sensor.black_mask,
                             elapsed_ms, left_count, right_count, rear_count);

// 条件控制逻辑
if (avoid_control.active) {
    commanded_forward = avoid_control.forward;
    commanded_lateral = avoid_control.lateral;    // 新增横向运动
    commanded_turn = avoid_control.turn;
} else {
    control = line_follow_update(&controller, sensor.black_mask,
                                 elapsed_ms, left_count, right_count);
    // 速度限制
    if (control.forward > avoid_control.tracking_forward_limit) {
        control.forward = avoid_control.tracking_forward_limit;
    }
    commanded_forward = control.forward;
    commanded_turn = control.turn;
}

result = drive_set_motion(commanded_forward, commanded_lateral,
                          commanded_turn, &wheels);
```

### 2. 配置文件扩展（`board_config.h`）

#### lnf_avoid项目新增配置

**超声波传感器配置：**
```c
// HC-SR04 ultrasonic ranger
#define ULTRASONIC_TRIG_GPIO         14
#define ULTRASONIC_ECHO_GPIO         13
#define ULTRASONIC_SAMPLE_PERIOD_MS  60
#define ULTRASONIC_ECHO_TIMEOUT_US   30000
```

**障碍物避让参数：**
```c
// One-shot obstacle avoidance
#define AVOID_TRIGGER_DISTANCE_MM      50
#define AVOID_TRIGGER_CONFIRM_SAMPLES   2
#define AVOID_SLOW_DISTANCE_MM         100
#define AVOID_SLOW_FORWARD              90
#define AVOID_CLEAR_DISTANCE_MM        120
#define AVOID_LATERAL_SPEED             300
#define AVOID_FORWARD_SPEED             140
#define AVOID_LEFT_MIN_COUNTS           240
#define AVOID_LEFT_MAX_COUNTS          2600
#define AVOID_FORWARD_TARGET_COUNTS    2200
#define AVOID_RIGHT_EXTRA_COUNTS        600
```

### 3. 新增组件

#### lnf_avoid项目独有文件：

**障碍物避让模块：**
- `obstacle_avoidance.c/h` - 障碍物检测与避让逻辑

**超声波传感器模块：**
- `ultrasonic.c/h` - 超声波传感器数据处理

### 4. 主循环控制差异

#### lnf_avoid项目增强功能

**1. 超声波传感器集成：**
```c
ultrasonic_reading_t ultrasonic = {0};
result = ultrasonic_get_latest(&ultrasonic);
```

**2. 避障状态管理：**
- 检测障碍物时暂停循迹
- 执行避让动作
- 避让完成后重置循迹控制器
```c
if (avoid_control.just_completed) {
    line_follow_init(&controller);
}
```

**3. 复杂的日志输出：**
```c
ESP_LOGI(TAG, "state=%s sensor=%s error=%d motion=[f=%d,l=%d,t=%d] "
             "wheels=[%d,%d,%d] enc=[%d,%d,%d] "
             "range=[%s,%umm,seq=%u] "
             "avoid=[state=%s progress=%lld outbound=%lld] "
             "search=[leg=%u dir=%d phase=%s progress=%lld/%lld] "
             "dt=%ums loop_max=%ums overruns=%u",
         line_follow_state_name(control.state), display, control.error,
         commanded_forward, commanded_lateral, commanded_turn,
         wheels.left, wheels.right, wheels.rear,
         left_count, right_count, rear_count,
         ultrasonic_status_name(ultrasonic.status),
         (unsigned)ultrasonic.distance_mm,
         (unsigned)ultrasonic.sequence,
         obstacle_avoidance_state_name(avoid_control.state),
         (long long)avoidance.progress_counts,
         (long long)avoidance.outbound_lateral_counts,
         // ... 更多调试信息
);
```

---

## 总结

### 核心循迹算法
两个项目的循迹核心算法**完全相同**，包括：
- PID控制参数
- 角检测和旋转逻辑
- 丢失线搜索策略
- 状态管理机制

### 关键差异
`lnf_avoid`项目相对于`line-following`项目的扩展：

1. **功能扩展**：
   - 增加了超声波障碍物检测
   - 实现了一键式障碍物避让
   - 支持横向平移运动

2. **控制逻辑增强**：
   - 条件控制（检测到障碍物时切换到避让模式）
   - 速度限制机制
   - 循迹自动重置

3. **传感器融合**：
   - 红外循迹传感器 + 超声波避障传感器
   - 多传感器数据融合控制

### 架构关系
```
lnf_avoid = line-following(基础循迹) + obstacle_avoidance(避障功能)
```

**结论**：`lnf_avoid`项目是在基础循迹功能的基础上增加了一个避让层，核心循迹算法保持不变，但增加了智能避障和横向运动能力，使小车能够应对更复杂的环境。